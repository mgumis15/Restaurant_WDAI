import { OwnCurrencyPipePipe } from './own-currency-pipe.pipe';

describe('OwnCurrencyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OwnCurrencyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
